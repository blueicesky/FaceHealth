# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FaceHealth' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FaceHealth
	pod 'Alamofire'
	pod 'SwiftyJSON'
	pod 'Firebase/Core'
	pod 'Firebase/Database'
	pod 'ResearchKit'
  target 'FaceHealthTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FaceHealthUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
